<h1 class="h2"><%=t('mining_type.text')%></h1>

<%= render 'form', mining_type: @mining_type %>

<%= link_to t('links.show'), @mining_type, class: 'btn btn-primary', role: 'button'%>
<%= link_to t('links.back'), mining_types_path, class: 'btn btn-primary', role: 'button'%>
