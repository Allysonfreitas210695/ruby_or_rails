<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('mining_type.description')%></strong>
  <%= @mining_type.description %>
</p>

<p>
  <strong><%= t('mining_type.acronym')%></strong>
  <%= @mining_type.acronym %>
</p>

<%= link_to t('links.edit'), edit_mining_type_path(@mining_type), class: 'btn btn-primary', role: 'button' %> 
<%= link_to t('links.back'), mining_types_path, class: 'btn btn-primary', role: 'button'%>
