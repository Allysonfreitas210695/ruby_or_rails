<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('coin.description') %></strong>
  <%= @coin.description %>
</p>

<p>
  <strong><%= t('coin.acronym') %></strong>
  <%= @coin.acronym %>
</p>

<p>
  <strong>Url image:</strong>
  <%= @coin.url_image %>
</p>

<%= link_to t('links.edit'), edit_coin_path(@coin), class: 'btn btn-primary', role: 'button'%>
<%= link_to t('links.back'), coins_path, class: 'btn btn-primary', role: 'button'%>
