<h1 class="h2"><%= t('coin.text')%></h1>

<%= render 'form', coin: @coin %>

<%= link_to t('links.edit'), coins_path, class: 'btn btn-primary', role: 'button'%>
<%= link_to t('links.back'), coins_path, class: 'btn btn-primary', role: 'button'%>
